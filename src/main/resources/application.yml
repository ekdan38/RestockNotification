spring:
  datasource:
    url: jdbc:mysql://db:3306/restocknotification?serverTimezone=UTC&characterEncoding=UTF-8
    username: user
    password: password

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
        show_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
